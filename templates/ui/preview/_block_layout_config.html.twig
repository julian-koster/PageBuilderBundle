<div class="inline-block border border-dashed rounded p-2 text-[10px] text-gray-700">
    <div class="flex flex-col items-center space-y-1">
        <!-- Margin Top -->
        <input name="mt" placeholder="mt" class="w-10 text-center border rounded-sm text-[10px] py-0.5" value="{{ this.layoutConfig(instanceId).mt|default(null) }}" data-instance-id="{{ instanceId }}" data-key="mt" data-action="change->admin--page-builder-preview#updateLayoutConfig" />

        <!-- Padding Box -->
        <div class="bg-gray-100 rounded p-1">
            <!-- Padding Top -->
            <div class="flex justify-center mb-1">
                <input name="pt" placeholder="pt" class="w-10 text-center border rounded-sm text-[10px] py-0.5" value="{{ this.layoutConfig(instanceId).pt|default(null) }}" data-instance-id="{{ instanceId }}" data-key="pt" data-action="change->admin--page-builder-preview#updateLayoutConfig" />
            </div>

            <div class="flex items-center">
                <!-- Margin Left -->
                <input name="ml" placeholder="ml" class="w-10 text-center border rounded-sm mr-1 text-[10px] py-0.5" value="{{ this.layoutConfig(instanceId).ml|default(null) }}" data-instance-id="{{ instanceId }}" data-key="ml" data-action="change->admin--page-builder-preview#updateLayoutConfig" />

                <!-- Padding Left -->
                <input name="pl" placeholder="pl" class="w-10 text-center border rounded-sm mr-1 text-[10px] py-0.5" value="{{ this.layoutConfig(instanceId).pl|default(null) }}" data-instance-id="{{ instanceId }}" data-key="pl" data-action="change->admin--page-builder-preview#updateLayoutConfig" />

                <!-- Content -->
                <div class="w-10 h-10 flex items-center justify-center border rounded-sm bg-white text-gray-400 text-[9px]">Content</div>

                <!-- Padding Right -->
                <input name="pr" placeholder="pr" class="w-10 text-center border rounded-sm ml-1 text-[10px] py-0.5" value="{{ this.layoutConfig(instanceId).pr|default(null) }}" data-instance-id="{{ instanceId }}" data-key="pr" data-action="change->admin--page-builder-preview#updateLayoutConfig" />

                <!-- Margin Right -->
                <input name="mr" placeholder="mr" class="w-10 text-center border rounded-sm ml-1 text-[10px] py-0.5" value="{{ this.layoutConfig(instanceId).mr|default(null) }}" data-instance-id="{{ instanceId }}" data-key="mr" data-action="change->admin--page-builder-preview#updateLayoutConfig" />
            </div>

            <!-- Padding Bottom -->
            <div class="flex justify-center mt-1">
                <input name="pb" placeholder="pb" class="w-10 text-center border rounded-sm text-[10px] py-0.5" value="{{ this.layoutConfig(instanceId).pb|default(null) }}" data-instance-id="{{ instanceId }}" data-key="pb" data-action="change->admin--page-builder-preview#updateLayoutConfig" />
            </div>
        </div>

        <!-- Margin Bottom -->
        <input name="mb" placeholder="mb" class="w-10 text-center border rounded-sm text-[10px] py-0.5" value="{{ this.layoutConfig(instanceId).mb|default(null) }}" data-instance-id="{{ instanceId }}" data-key="mb" data-action="change->admin--page-builder-preview#updateLayoutConfig" />
    </div>
</div>